package ca.quickheaven.ocjp.practice.test.two;

public class Question31 {

    // The BaseStream.isParallel method returns whether this stream, if a terminal operation were to be executed, would
    // execute in parallel.
}
