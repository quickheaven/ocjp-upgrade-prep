package ca.quickheaven.ocjp.practice.test.two;

public class Question1 {

    /*
     * Fork/Join Framework:
     * A ForkJoinPool begins execution when it commenced in the ForkJoinPool.commonPool() via fork(), invoke(), or related methods if it is not explicitly submitted to ForkJoinPool.
     * Idle thread will attempt to steal task from other threads, or be blocked if there are no tasks available rather than released.
     * Framework works on a work-stealing algorithm.
     */
}
