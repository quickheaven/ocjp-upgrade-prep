package ca.quickheaven.ocjp.practice.test.one;

public class Question5 {

    // The implementation of a subclass of a RecursiveAction, which does not return a value. Therefore, the join operation is redundant
}
