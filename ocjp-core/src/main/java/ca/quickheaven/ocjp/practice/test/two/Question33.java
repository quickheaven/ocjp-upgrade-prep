package ca.quickheaven.ocjp.practice.test.two;

public class Question33 {

    /*
     * Only intermediate operations are lazy. Terminal operations are implemented right after invoked.
     * Stream is functional in nature. An operation on a stream produces a result, but does not modify it source.
     * The BaseStream.sequential method can be invoked on a stream to make it sequential after parallelism.
     * A Stream cannot be reused after a terminal operation is called.
     */
}
