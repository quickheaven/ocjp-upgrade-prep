package ca.quickheaven.ocjp.practice.test.two;

public class Question37 {

    /*
     * The 'putIfAbsent method' puts a whole collection value to the map rather than adding a new element to an existing value.
     * The 'compute' method re-calculates entries of the map regardless of value existence.
     * The 'replace' method takes action only when a value is already existent.
     * The replaceIfAbsent method does not exist.
     *
     */
}
