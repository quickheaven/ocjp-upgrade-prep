package ca.quickheaven.ocjp.practice.test.one;

public class Question57 {

    // DateFormat constructor does not exist.
    // DateFormat.getDateInstance method has three flavors but none of them takes a Locale as its single argument.
    // DateFormat.setLocal is not existent
}
