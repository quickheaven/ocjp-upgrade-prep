package ca.quickheaven.ocjp.practice.test.two;

public class Question23 {

    /**
     * StandardOpenOption
     * DESYNC - requires that every update to the file's content be written synchronously to the underlying storage device.
     *
     * SYNC -  requires that every update to the file's content or metadata be written synchronously to the underlying storage device.
     */
}
