/**
 * Introducing Functional Programming from Chapter 4. Bi means two. It comes
 * from Latin, but you can remember it from English words like binary (0 or 1)
 * or bicycle (two wheels). Always add another parameter when you see Bi show
 * up.
 *
 * A stream in Java is a sequence of data. A stream pipeline is the operations that run on a stream to produce result.
 * Think of a stream pipeline as an assembly line in a factory.
 */
package ca.quickheaven.ocjp.func.prog.ch04;